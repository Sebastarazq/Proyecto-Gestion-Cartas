doctype html
html(lang='es')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width,initial-scale=1')
    title Actualizar Arma
    link(rel='stylesheet', href='/css/crearCarta.css')
  body
    .banner
      main
        img(src='/img/banner.png', alt='banner')
        h1 ACTUALIZACIÓN DE ARMA
    .container
      header
        h1 Actualizar Arma
      form.card-form(action=`/admin/actualizararma/${arma._id}`, method='post', enctype='multipart/form-data')
        label(for='urlImagen')
          span.custom-upload-button Subir imagen:
          input(type='file', name='urlImagen', id='urlImagen', accept='image/*', required, style='position: absolute; opacity: 0;')
          span.file-name(id='file-name-span')
        label(for='nombre') Nombre:
        input(type='text', name='nombre', id='nombre', required, value=arma.nombre)
        label(for='tipoHeroe') Tipo de Héroe:
        select(name='tipoHeroe', id='tipoHeroe', required)
          option(value='Guerrero Tanque', selected=arma.tipoHeroe === 'Guerrero Tanque') Guerrero Tanque
          option(value='Guerrero Armas', selected=arma.tipoHeroe === 'Guerrero Armas') Guerrero Armas
          option(value='Mago Fuego', selected=arma.tipoHeroe === 'Mago Fuego') Mago Fuego
          option(value='Mago Hielo', selected=arma.tipoHeroe === 'Mago Hielo') Mago Hielo
          option(value='Pícaro Veneno', selected=arma.tipoHeroe === 'Pícaro Veneno') Pícaro Veneno
          option(value='Pícaro Machete', selected=arma.tipoHeroe === 'Pícaro Machete') Pícaro Machete
        label(for='efecto.case') Efecto - Case:
        input(type='number', name='efecto.case', id='efectoCase', required, value=arma.efecto.case)
        label(for='efecto.statEffect') Efecto - Stat Effect:
        input(type='number', name='efecto.statEffect', id='efectoStatEffect', required, value=arma.efecto.statEffect)
        label(for='efecto.stat') Efecto - Stat:
        input(type='text', name='efecto.stat', id='efectoStat', required, value=arma.efecto.stat)
        label(for='efecto.target') Efecto - Target:
        input(type='text', name='efecto.target', id='efectoTarget', required, value=arma.efecto.target)
        label(for='efecto.turnCount') Efecto - Turn Count:
        input(type='number', name='efecto.turnCount', id='efectoTurnCount', required, value=arma.efecto.turnCount)
        label(for='activo') Activo:
        select(name='activo', id='activo', required)
          option(value='true', selected=arma.activo) Activo
          option(value='false', selected=!arma.activo) Inactivo
        label(for='desc') Descripción:
        textarea(name='desc', id='desc', required)= arma.desc
        button(type='submit') Actualizar Arma
  script(src="/js/scriptImagen.js")
